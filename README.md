# LLM tokenizer based on the ant colony optimization with a genetic algorithm mechanism instead of evaporation.
idk the idea sounds cool but it sucks. just use Viberti algorithm.

## How to train
run this command
```
 cargo run --bin tokant --release    
```
## How to test
run this command
```
 cargo run --release --bin tokenize "the ant colony optimization algorithm is very interesting to observe"
```